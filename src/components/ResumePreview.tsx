const ResumePreview: React.FC = () => {
    return (
      <div>
        {/* Your component logic */}
        <h1>Resume Preview</h1>
        {/* Display the resume preview here */}
      </div>
    );
  };
  
  export default ResumePreview; // Add this line if it wasn't already present
    